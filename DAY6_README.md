# DAY6: File I/O (csv --> json)

Goal: Read a csv, compute simple stats, and export results to json.

## Files
- sample.csv: sample input data
- analyze_csv.py: analyzer script (csv --> json)

## Run
python analyze_csv.py --in sample.csv --out result.json --unique-col device_id
